<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.sale.purchase.dao.PurchaseDao">
	<resultMap type="purchase" id="purchaseMap">
		<result column="purchase_id" />
		<result column="purchase_date"/>
		<result column="product_kg" />
		<result column="purchase_count"/>
		<result column="product_id" />
		<result column="product_type_id" />
		<result column="product_type_name" />
		<result column="product_name" />
		
	</resultMap>
	
<select id="purchaseList" resultType="purchase">
	select 
		purchase_id,
		TO_CHAR(purchase_date, 'YYYY-MM-DD') AS purchase_date,
		product_kg,
		purchase_count,
		product_id,
		product_type_id,
		product_type_name,
		product_name
		from 
		purchase
</select>

<select id="purchaseCnt" resultType="int">
	SELECT COUNT(*)
	FROM purchase
</select>

<select id="purchaseDetail" resultType="com.example.demo.sale.purchase.vo.PurchaseVO">
    SELECT
        purchase_id,
        TO_CHAR(purchase_date, 'YYYY-MM-DD') AS purchase_date,
        product_kg,
        purchase_count,
        product_id,
        product_name,
        product_type_id,
        product_type_name
    FROM
        purchase
    WHERE
        purchase_id = #{purchase_id}
</select>

<insert id="purchaseSave">
    <selectKey resultType="int" keyProperty="purchase_id" order="BEFORE">
        SELECT PURCHASE_SEQ.NEXTVAL FROM DUAL
    </selectKey>
    insert into purchase
    (
        purchase_id,
        purchase_date,
        product_kg,
        purchase_count,
        product_name
    )
    values (
        PURCHASE_SEQ.CURRVAL, -- PURCHASE_ID에 대한 시퀀스 값 사용
        TO_DATE(#{purchase_date}, 'YYYY-MM-DD'),
        #{product_kg},
        #{purchase_count},
        #{product_name}
    )
</insert>

<update id="purchaseUpdate">
	update purchase
	set
		purchase_date = TO_DATE(#{purchase_date}, 'YYYY-MM-DD'),
		product_kg = #{product_kg},
		purchase_count = #{purchase_count},
		product_name = #{product_name}
	where 
		purchase_id = #{purchase_id}
</update>

<delete id="deletePurchase">
	delete from purchase
	where purchase_id = #{purchase_id}
</delete>



</mapper>